# วิธีอัปเดตระบบ CP9X ให้เป็นปัจจุบันทั้งหมด (ทำตามทีละขั้น)

> เอกสารนี้เขียนไว้ให้ทำตามได้ทันทีโดยไม่ต้องรู้เรื่องเทคนิคมาก่อน
> อ่านจากบนลงล่าง ทำทีละข้อ ห้ามข้าม

---

## สรุปก่อนว่าตอนนี้สถานการณ์เป็นยังไง

ระบบ CP9X มี **2 ส่วนแยกกัน** ที่ต้องอัปเดตคนละวิธี:

| ส่วน | คืออะไร | อัปเดตยังไง | ตอนนี้ค้างอยู่แค่ไหน |
|---|---|---|---|
| **1. เซิร์ฟเวอร์** (Supabase Edge Function) | สมองของระบบ อยู่บนคลาวด์ ทุกคนใช้ตัวเดียวกัน | คัดลอกโค้ดไปวางบนเว็บ Supabase แล้วกด Deploy | **ค้าง 3 รอบ** (v1.0.22, v1.0.23, v1.0.24) |
| **2. ตัวโปรแกรมบนเครื่อง** (แอป CP9X) | หน้าตาที่ผู้ใช้เห็น ติดตั้งแยกในเครื่องแต่ละคน | push ขึ้น GitHub แล้วระบบ build + ส่งอัปเดตให้ทุกเครื่องเอง | **ค้าง 3 รอบ** (v1.0.26, v1.0.27, v1.0.28) |

**ทำไมเวอร์ชันไม่ตรงกัน:** ตอนนี้ไฟล์ในเครื่องคุณเป็น **1.0.28** แล้ว แต่บน GitHub ปล่อยไว้ล่าสุดแค่ **1.0.25** เพราะแก้โค้ดกันมาหลายรอบโดยยังไม่ได้ push ขึ้นไป

**เป้าหมายของเอกสารนี้:** ทำให้ทุกอย่างมาอยู่ที่ **1.0.28** พร้อมกันหมด

---

# ขั้นที่ 1 — อัปเดตเซิร์ฟเวอร์ (Supabase Edge Function)

> ⏱ ใช้เวลาประมาณ 3 นาที · ทำก่อนขั้นที่ 2 เสมอ
> ทำครั้งเดียวจบ ครอบคลุมทั้ง 3 รอบที่ค้างอยู่ (v1.0.22 + v1.0.23 + v1.0.24)

### 1.1 เปิดไฟล์โค้ดเซิร์ฟเวอร์

เปิด File Explorer ไปที่โฟลเดอร์นี้:

```
C:\Users\Haneef\Downloads\ใบวางบิลอัพเกรด\cp9x-desktop\supabase-edge-function\
```

คลิกขวาที่ไฟล์ **`index.ts`** → เลือก **Open with** → **Notepad** (หรือโปรแกรมแก้ข้อความอะไรก็ได้)

### 1.2 คัดลอกโค้ดทั้งหมด

ในหน้าต่าง Notepad ที่เปิดขึ้นมา:

1. กด **Ctrl + A** (เลือกทั้งหมด)
2. กด **Ctrl + C** (คัดลอก)

### 1.3 เปิดหน้า Supabase

เปิดเบราว์เซอร์ไปที่ลิงก์นี้ (ต้องล็อกอิน Supabase ก่อน):

```
https://supabase.com/dashboard/project/hefnjozijflnhdunmewl/functions/app-api
```

### 1.4 วางโค้ดแล้ว Deploy

1. กดปุ่ม **Edit function** (มุมขวาบน)
2. คลิกในช่องโค้ดที่แสดงอยู่ → กด **Ctrl + A** (เลือกโค้ดเก่าทั้งหมด)
3. กด **Ctrl + V** (วางทับด้วยโค้ดใหม่ที่คัดลอกมา)
4. กดปุ่ม **Deploy** แล้วรอจนขึ้นข้อความว่าสำเร็จ (ประมาณ 30 วินาที - 1 นาที)

### ✅ เช็คว่าขั้นที่ 1 สำเร็จไหม

เปิดแอป CP9X แล้วลองทำสิ่งเหล่านี้:

- เข้าเมนู **"รายงานสถานะดำเนินการ"** → กดโหลดข้อมูล → ต้องเห็นคอลัมน์ **"เลขทรัพย์สิน"** เพิ่มขึ้นมาใหม่
- ถ้าเห็นคอลัมน์นี้ = เซิร์ฟเวอร์อัปเดตเรียบร้อยแล้ว ✓

> ⚠️ **ถ้ายังไม่เห็นคอลัมน์ใหม่:** ลองรอ 1-2 นาทีแล้วกดโหลดข้อมูลใหม่อีกครั้ง (Supabase ใช้เวลาอัปเดตสักครู่)

---

# ขั้นที่ 2 — ปล่อยอัปเดตตัวโปรแกรมให้ทุกเครื่อง

> ⏱ พิมพ์คำสั่ง 2 นาที + รอระบบ build เอง 5-10 นาที
> ทำแค่ที่เครื่องคุณเครื่องเดียว เครื่องคนอื่นจะได้รับอัปเดตเองอัตโนมัติ

### 2.1 เปิด PowerShell ที่โฟลเดอร์โปรเจกต์

**วิธีที่เร็วที่สุด:**

1. เปิด File Explorer ไปที่โฟลเดอร์:
   ```
   C:\Users\Haneef\Downloads\ใบวางบิลอัพเกรด\cp9x-desktop
   ```
2. คลิกที่ **แถบที่อยู่ด้านบนสุด** (ช่องที่แสดง path)
3. พิมพ์ทับลงไปว่า `powershell` แล้วกด **Enter**
4. หน้าต่างสีน้ำเงิน/ดำจะเปิดขึ้นมา — พร้อมใช้งานแล้ว

### 2.2 พิมพ์คำสั่งทีละบรรทัด

พิมพ์ (หรือคัดลอกไปวาง) **ทีละบรรทัด** กด Enter แล้วรอให้เสร็จก่อนค่อยพิมพ์บรรทัดถัดไป:

```powershell
git add -A
```
> เก็บไฟล์ที่แก้ไขทั้งหมดเตรียมส่ง

```powershell
git commit -m "v1.0.28"
```
> บันทึกการเปลี่ยนแปลงพร้อมชื่อกำกับ

```powershell
git push origin main
```
> ส่งโค้ดขึ้น GitHub

```powershell
git tag v1.0.28
```
> ติดป้ายเวอร์ชัน (**ต้องเป็น `v1.0.28` เป๊ะ ๆ** ห้ามพิมพ์เลขอื่น)

```powershell
git push origin v1.0.28
```
> ส่งป้ายเวอร์ชันขึ้นไป — **บรรทัดนี้คือตัวสั่งให้ระบบเริ่ม build อัตโนมัติ**

### 2.3 รอระบบ build ให้เอง

เปิดเบราว์เซอร์ไปที่:

```
https://github.com/chollachat89/cp9x-desktop/actions
```

- จะเห็นรายการงานที่กำลังทำงานอยู่ มี **วงกลมสีเหลืองหมุน ๆ** = กำลัง build
- รอประมาณ **5-10 นาที**
- พอขึ้น **เครื่องหมายถูกสีเขียว ✓** = ปล่อยอัปเดตสำเร็จแล้ว

### ✅ เช็คว่าขั้นที่ 2 สำเร็จไหม

เปิดลิงก์นี้:

```
https://github.com/chollachat89/cp9x-desktop/releases
```

ต้องเห็นรายการบนสุดเป็น **v1.0.28** และมีไฟล์แนบอยู่ 2 ไฟล์:

- `CP9X-Setup-1.0.28.exe`
- `latest.yml`

ถ้าครบตามนี้ = เรียบร้อย ✓

---

# ขั้นที่ 3 — อัปเดตเครื่องตัวเอง

เครื่องของคุณจะยังใช้เวอร์ชันเก่าอยู่จนกว่าจะติดตั้งใหม่ เลือกทำอย่างใดอย่างหนึ่ง:

**วิธี A (ง่ายสุด):** เปิดแอป CP9X ทิ้งไว้ รอไม่เกิน 30 นาที ระบบจะดาวน์โหลดอัปเดตให้เอง แล้วขึ้นแถบแจ้งเตือนมุมขวาล่างให้กด "รีสตาร์ทเพื่ออัปเดตตอนนี้"

**วิธี B (เร็วกว่า):** ดาวน์โหลด `CP9X-Setup-1.0.28.exe` จากหน้า Releases ด้านบน แล้วติดตั้งทับตัวเดิมได้เลย (ไม่ต้องถอนตัวเก่าออก)

---

# ขั้นที่ 4 — เครื่องคนอื่นจะอัปเดตเองอัตโนมัติ

**ไม่ต้องทำอะไรเลย** — เครื่องที่ติดตั้งแอปไว้แล้วจะ:

1. ตรวจเจอเวอร์ชันใหม่เองภายใน **30 นาที** (เช็คทุก 30 นาทีขณะเปิดแอปอยู่)
2. ดาวน์โหลดให้อัตโนมัติเบื้องหลัง (ใช้งานต่อได้ตามปกติระหว่างรอ)
3. พอโหลดเสร็จจะขึ้น **แถบแจ้งเตือนมุมขวาล่าง** พร้อมปุ่ม **"รีสตาร์ทเพื่ออัปเดตตอนนี้"**
4. ถ้าผู้ใช้ไม่กด ก็จะอัปเดตให้เองอัตโนมัติตอนปิดโปรแกรมครั้งถัดไป

> 💡 **ถ้าอยากให้ใครอัปเดตทันทีไม่ต้องรอ:** บอกให้เขากดปุ่ม **"ตรวจอัปเดต"** ที่มุมล่างซ้ายของแอป (ใต้ชื่อผู้ใช้)

> ⚠️ **หมายเหตุ:** แถบแจ้งเตือนกับเลขเวอร์ชันมุมล่างซ้ายเป็นของใหม่ใน v1.0.28 — คนที่ยังอยู่เวอร์ชันเก่ากว่านี้จะยังไม่เห็น แต่จะยังได้รับอัปเดตแบบเงียบตอนปิดโปรแกรมเหมือนเดิม พอขึ้นมาถึง v1.0.28 แล้วรอบต่อ ๆ ไปถึงจะเห็นครบ

---

# วิธีเช็คว่าใครอัปเดตแล้วบ้าง

ให้ดูที่ **มุมล่างซ้ายของแอป ใต้ชื่อผู้ใช้** จะมีข้อความว่า `เวอร์ชัน 1.0.28`

- ตรงกับเลขบนหน้า GitHub Releases = อัปเดตแล้ว ✓
- เลขน้อยกว่า = ยังไม่อัปเดต ให้กดปุ่ม "ตรวจอัปเดต" ข้าง ๆ

---

# 📌 กฎเหล็กสำหรับรอบต่อ ๆ ไป (อ่านเพื่อไม่ให้เวอร์ชันเพี้ยนอีก)

**เลข 4 จุดนี้ต้องเท่ากันเสมอ:**

```
package.json  =  git tag  =  GitHub Releases  =  เลขที่โชว์ในแอป
   1.0.28     =  v1.0.28  =     v1.0.28       =      1.0.28
```

**ลำดับการทำที่ถูกต้องทุกครั้ง:**

1. แก้โค้ด
2. แก้เลข `version` ใน `package.json` (เช่น เป็น `1.0.29`)
3. `git add -A` → `git commit` → `git push origin main`
4. `git tag v1.0.29` → `git push origin v1.0.29`
5. ถ้ามีการแก้ไฟล์ `supabase-edge-function/index.ts` ด้วย → อย่าลืม Deploy ที่ Supabase (ขั้นที่ 1)

**ระบบกันพลาดที่ติดตั้งไว้ให้แล้ว:** ถ้าเผลอ push tag ที่เลขไม่ตรงกับ `package.json` ระบบ build จะหยุดทันทีพร้อมขึ้นข้อความสีแดงบอกว่าเลขไหนไม่ตรง และต้องแก้เป็นเลขอะไร — ปล่อยเวอร์ชันผิดขึ้นไปไม่ได้อีกแล้ว

---

# 🔧 ถ้าติดปัญหา

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| `git push` ขึ้น `could not read Username` | ยังไม่ได้ล็อกอิน GitHub บนเครื่อง → ติดตั้ง [Git for Windows](https://git-scm.com/download/win) แล้วลอง push ใหม่ จะมีหน้าต่างให้ล็อกอิน |
| Actions ขึ้นกากบาทแดง ❌ ที่ขั้น "ตรวจสอบว่า tag ตรงกับ version" | เลข tag ไม่ตรงกับ `package.json` → อ่านข้อความสีแดงว่าให้แก้เป็นเลขอะไร แล้วทำตาม |
| Actions ขึ้นกากบาทแดง ❌ ที่ขั้น "Publish to GitHub Releases" พร้อมคำว่า `already_exists` | รัน workflow ซ้ำกับ tag เดิมที่ปล่อยสำเร็จไปแล้ว → **ไม่ใช่ปัญหา ปล่อยผ่านได้** ไปเช็คหน้า Releases ว่าไฟล์ครบไหม ถ้าครบคือเรียบร้อย |
| `git tag v1.0.28` ขึ้นว่า `already exists` | เคยติดป้ายเลขนี้ไว้แล้ว → ลบทิ้งก่อนด้วย `git tag -d v1.0.28` แล้ว `git push origin :refs/tags/v1.0.28` จากนั้นเริ่มขั้น 2.2 ใหม่ |
| เครื่องคนอื่นไม่ยอมอัปเดตสักที | ให้เขากดปุ่ม "ตรวจอัปเดต" มุมล่างซ้าย · ถ้ายังไม่ได้ ให้ดาวน์โหลด `.exe` จากหน้า Releases ไปติดตั้งทับเลย |
