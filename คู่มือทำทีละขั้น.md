# CP9X Desktop — คู่มือทำทีละขั้น (ละเอียด)

เอกสารนี้พาทำตั้งแต่เครื่องเปล่า จนได้ `.exe` ที่อัปเดตตัวเองได้จริง
ทำตามลำดับ ห้ามข้าม **ส่วนที่ 0** และ **ส่วนที่ 3**

สารบัญ

- [ส่วนที่ 0 — เตรียมเครื่อง](#ส่วนที่-0--เตรียมเครื่อง)
- [ส่วนที่ 1 — รันทดสอบครั้งแรก](#ส่วนที่-1--รันทดสอบครั้งแรก)
- [ส่วนที่ 2 — Build เป็น .exe](#ส่วนที่-2--build-เป็น-exe)
- [ส่วนที่ 3 — เชื่อม GitHub (หัวใจของ auto-update)](#ส่วนที่-3--เชื่อม-github-หัวใจของ-auto-update)
- [ส่วนที่ 4 — ปล่อยเวอร์ชันแรก v1.0.0](#ส่วนที่-4--ปล่อยเวอร์ชันแรก-v100)
- [ส่วนที่ 5 — ทดสอบ auto-update ให้เห็นกับตา](#ส่วนที่-5--ทดสอบ-auto-update-ให้เห็นกับตา)
- [ส่วนที่ 6 — งานประจำวัน: แก้โค้ด → ปล่อยอัปเดต](#ส่วนที่-6--งานประจำวัน-แก้โค้ด--ปล่อยอัปเดต)
- [ส่วนที่ 7 — เข้าใจว่ามันทำงานยังไง](#ส่วนที่-7--เข้าใจว่ามันทำงานยังไง)
- [ส่วนที่ 8 — แก้ปัญหา](#ส่วนที่-8--แก้ปัญหา)
- [ส่วนที่ 9 — ปรับแต่งเพิ่มเติม](#ส่วนที่-9--ปรับแต่งเพิ่มเติม)

---

## ส่วนที่ 0 — เตรียมเครื่อง

ทำครั้งเดียว บนเครื่อง Windows

### 0.1 ติดตั้ง Node.js

1. เปิด https://nodejs.org
2. โหลดปุ่มฝั่งซ้าย **LTS** (เลข 20.x หรือ 22.x)
3. ติดตั้งแบบกด Next รวด — **ติ๊กช่อง "Add to PATH"** ไว้ (ปกติติ๊กมาให้แล้ว)
4. ปิด PowerShell/CMD ที่เปิดค้างอยู่ทั้งหมด แล้วเปิดใหม่ (ไม่งั้นจะยังไม่รู้จักคำสั่ง)

ตรวจว่าติดตั้งสำเร็จ — เปิด PowerShell พิมพ์:

```powershell
node -v
npm -v
```

ต้องขึ้นเลขเวอร์ชันทั้งสองบรรทัด เช่น `v22.11.0` และ `10.9.0`
ถ้าขึ้น *"ไม่รู้จักคำสั่ง"* แปลว่ายังไม่ได้ปิด-เปิดหน้าต่างใหม่ หรือ PATH ไม่ติด

### 0.2 ติดตั้ง Git

1. เปิด https://git-scm.com/download/win → โหลด 64-bit
2. ติดตั้งกด Next รวดได้เลย (ค่า default ปลอดภัยหมด)
3. ตรวจ: `git --version`

### 0.3 ตั้งชื่อผู้ใช้ git (ครั้งเดียวตลอดชีพ)

```powershell
git config --global user.name "Chollachat C."
git config --global user.email "chollachat-c@rmutp.ac.th"
```

### 0.4 มีบัญชี GitHub

ถ้ายังไม่มี สมัครที่ https://github.com/signup — จำ **username** ไว้ จะใช้ในส่วนที่ 3

---

## ส่วนที่ 1 — รันทดสอบครั้งแรก

### 1.1 เปิด PowerShell ที่โฟลเดอร์โปรเจกต์

วิธีง่ายสุด: เปิด File Explorer ไปที่โฟลเดอร์ `cp9x-desktop`
คลิกที่ช่อง address bar ด้านบน พิมพ์ `powershell` แล้ว Enter

หรือพิมพ์เอง:

```powershell
cd "$env:USERPROFILE\Downloads\ใบวางบิลอัพเกรด\cp9x-desktop"
```

### 1.2 ติดตั้ง dependencies

```powershell
npm install
```

รอ 1–3 นาที (โหลด Electron ~100 MB) จะได้โฟลเดอร์ `node_modules` และไฟล์ `package-lock.json`

> `package-lock.json` **ต้อง commit ขึ้น git ด้วย** — มันล็อกเวอร์ชันให้ build บน GitHub เหมือนบนเครื่องคุณเป๊ะ

### 1.3 ลองเปิดแอป

```powershell
npm run dev
```

ควรเห็น:

- หน้าต่างแอปเปิดขึ้นมาแสดงหน้า CP9X เหมือนในเบราว์เซอร์
- มุมซ้ายล่างมีตัวเลขจาง ๆ `v1.0.0`
- DevTools เปิดแยกหน้าต่าง (เฉพาะโหมด dev)

**เช็คสำคัญ:** ลองล็อกอินหรือกดปุ่มที่ต้องคุยกับ Supabase
ถ้าใช้ได้ = การเชื่อมต่อ Edge Function ผ่าน `file://` ทำงานปกติ
ถ้าติด CORS จะเห็น error สีแดงใน DevTools tab Console — ดู [ส่วนที่ 8](#88-ติด-cors-เรียก-supabase-ไม่ได้)

ปิดแอปด้วยการกดกากบาท หรือ `Ctrl+C` ใน PowerShell

> ในโหมด dev แอปจะ **ไม่** ตรวจหาอัปเดต — ตั้งใจให้เป็นแบบนั้น

---

## ส่วนที่ 2 — Build เป็น .exe

### 2.1 สั่ง build

```powershell
npm run build
```

หรือดับเบิลคลิกไฟล์ `build-exe.bat` (มันจะ `npm install` ให้เองถ้ายังไม่มี)

ครั้งแรกจะช้าหน่อย เพราะต้องโหลด NSIS toolchain มา cache ไว้

### 2.2 ผลลัพธ์ในโฟลเดอร์ `dist\`

| ไฟล์ | คืออะไร | ต้องมีตอนปล่อย release ไหม |
|---|---|---|
| `CP9X-Setup-1.0.0.exe` | ตัวติดตั้งที่เอาไปแจกได้ | **ต้องมี** |
| `CP9X-Setup-1.0.0.exe.blockmap` | แผนที่ไฟล์ ให้ดาวน์โหลดอัปเดตเฉพาะส่วนที่ต่าง | **ต้องมี** |
| `latest.yml` | ไฟล์บอกเวอร์ชันล่าสุด — แอปอ่านไฟล์นี้เพื่อรู้ว่ามีของใหม่ | **ต้องมี ห้ามลืม** |
| `win-unpacked\` | แอปแบบยังไม่บีบ ไว้ debug | ไม่ต้อง |

### 2.3 ลองติดตั้ง

ดับเบิลคลิก `CP9X-Setup-1.0.0.exe`

- Windows จะขึ้นจอฟ้า **"Windows protected your PC"** — ปกติ เพราะยังไม่ได้เซ็นโค้ด
  กด **More info** → **Run anyway**
- ตัวติดตั้งจะให้เลือกโฟลเดอร์ได้ (ตั้งไว้แบบนั้นใน `nsis.allowToChangeInstallationDirectory`)
- ติดตั้งลงที่ `%LOCALAPPDATA%\Programs\CP9X\` และสร้าง shortcut บน Desktop

เปิดจาก shortcut แล้วดูมุมซ้ายล่างว่าขึ้น `v1.0.0`

---

## ส่วนที่ 3 — เชื่อม GitHub (หัวใจของ auto-update)

ถ้าข้ามส่วนนี้ แอปจะทำงานได้ แต่ **จะอัปเดตตัวเองไม่ได้**

### 3.1 สร้าง repository

1. เข้า https://github.com/new
2. **Repository name:** `cp9x-desktop` (ต้องตรงกับที่จะใส่ในไฟล์ config)
3. เลือก **Public**
   - Public = แอปดึงอัปเดตได้เลย ไม่ต้องใส่ token ในตัวแอป
   - ถ้าเลือก Private ต้องฝัง token ไว้ในแอป ซึ่ง**ไม่ปลอดภัย** — แนะนำ Public
   - กังวลเรื่องโค้ดรั่ว? โค้ดฝั่งหน้าเว็บถูกแกะดูได้อยู่แล้วไม่ว่าจะทางไหน สิ่งที่ต้องกันคือ RLS ฝั่ง Supabase (ดู [9.4](#94-ความปลอดภัย-supabase))
4. **อย่า**ติ๊ก Add README / .gitignore / license (โปรเจกต์เรามีแล้ว)
5. กด **Create repository**

### 3.2 ใส่ชื่อ GitHub ลงไฟล์ config

เปิด `electron-builder.yml` ด้วย Notepad หรือ VS Code หาบรรทัดนี้:

```yaml
publish:
  - provider: github
    owner: CHANGE_ME_GITHUB_USER      # <-- แก้บรรทัดนี้
    repo: cp9x-desktop
    releaseType: release
```

เปลี่ยน `CHANGE_ME_GITHUB_USER` เป็น username GitHub ของคุณ เช่น

```yaml
    owner: haneef-c
```

> ต้องเป็น **username** ไม่ใช่ชื่อที่แสดง ไม่ใช่อีเมล
> ดูได้จาก URL หน้าโปรไฟล์: `https://github.com/haneef-c` → `haneef-c`

**บันทึกไฟล์** แล้ว build ใหม่หนึ่งรอบ (`npm run build`) เพื่อให้ค่านี้ฝังเข้าไปในแอป

### 3.3 push โค้ดขึ้น GitHub

```powershell
cd "$env:USERPROFILE\Downloads\ใบวางบิลอัพเกรด\cp9x-desktop"

git init
git branch -M main
git add -A
git commit -m "CP9X Desktop v1.0.0"
git remote add origin https://github.com/USERNAME/cp9x-desktop.git
git push -u origin main
```

แทน `USERNAME` ด้วยชื่อของคุณ

ครั้งแรกจะเด้งหน้าต่างให้ล็อกอิน GitHub — ล็อกอินผ่าน browser ได้เลย

**ตรวจ:** เปิดหน้า repo ใน browser ควรเห็นไฟล์ครบ และ **ต้องไม่มี** โฟลเดอร์ `node_modules` กับ `dist` (`.gitignore` กันไว้แล้ว)
ต้องเห็น `package-lock.json` ด้วย

---

## ส่วนที่ 4 — ปล่อยเวอร์ชันแรก v1.0.0

มี 2 ทาง เลือกทางใดทางหนึ่ง

### ทาง A — ให้ GitHub build ให้ (แนะนำ)

ไม่ต้องเปิดเครื่องรอ ไม่ต้องมี token

```powershell
git tag v1.0.0
git push origin v1.0.0
```

ไปดูที่แท็บ **Actions** ในหน้า repo — จะเห็นงานชื่อ *Build & Release CP9X* กำลังรัน
ใช้เวลา 4–8 นาที

เสร็จแล้วไปแท็บ **Releases** จะเห็น release `v1.0.0` พร้อมไฟล์แนบ 3 ไฟล์:
`CP9X-Setup-1.0.0.exe` / `.blockmap` / `latest.yml`

**ถ้า Actions ขึ้นสีแดง** → ดู [8.5](#85-github-actions-ล้มเหลว)

### ทาง B — build เองแล้ว upload

**B-1. สร้าง Personal Access Token**

1. https://github.com/settings/tokens?type=beta → **Generate new token**
2. Token name: `electron-builder`
3. Expiration: เลือกตามสะดวก (90 วัน หรือ No expiration)
4. Repository access: **Only select repositories** → เลือก `cp9x-desktop`
5. Permissions → Repository permissions → หา **Contents** → เปลี่ยนเป็น **Read and write**
6. กด **Generate token** → **คัดลอกเก็บไว้ทันที** (ปิดหน้าแล้วดูไม่ได้อีก)

**B-2. สั่ง publish**

```powershell
$env:GH_TOKEN = "github_pat_xxxxxxxxxxxxx"
npm run release
```

electron-builder จะ build แล้ว upload ขึ้น Release ให้เอง

> `$env:GH_TOKEN` มีผลเฉพาะหน้าต่าง PowerShell นั้น ปิดแล้วหาย — ปลอดภัยดี

---

## ส่วนที่ 5 — ทดสอบ auto-update ให้เห็นกับตา

ขั้นนี้สำคัญ ทำครั้งเดียวเพื่อพิสูจน์ว่าระบบเดินจริง

### 5.1 ติดตั้งเวอร์ชันเก่าไว้ก่อน

โหลด `CP9X-Setup-1.0.0.exe` จากหน้า Releases มาติดตั้ง แล้ว**ปิดแอปไว้ก่อน**

### 5.2 ทำเวอร์ชันใหม่

แก้ `package.json` บรรทัดที่ 4:

```json
"version": "1.0.1",
```

ลองแก้อะไรในหน้าเว็บนิดหน่อยด้วย เพื่อให้เห็นความต่าง เช่น เปิด `src/renderer/index.html` แก้ `<title>CP9X</title>` เป็น `<title>CP9X 1.0.1</title>`

### 5.3 ปล่อย

```powershell
git add -A
git commit -m "v1.0.1"
git tag v1.0.1
git push origin main --tags
```

รอ Actions build เสร็จ (เช็คแท็บ Actions ให้ขึ้นติ๊กเขียว)

### 5.4 เปิดแอป v1.0.0 ที่ติดตั้งไว้

นับ 1 ถึง 5 แล้วดู:

| เวลา | สิ่งที่ควรเห็น |
|---|---|
| ~5 วินาที | แถบดำมุมขวาล่าง **"มีเวอร์ชันใหม่ — เวอร์ชัน 1.0.1 กำลังดาวน์โหลดอัตโนมัติ..."** |
| ถัดมา | แถบเปลี่ยนเป็น **"กำลังดาวน์โหลดอัปเดต"** พร้อมหลอดเขียว-ฟ้าวิ่ง % |
| ดาวน์โหลดเสร็จ | **"พร้อมติดตั้ง"** + ปุ่ม **รีสตาร์ตตอนนี้** / **ไว้ทีหลัง** |

กด **รีสตาร์ตตอนนี้** → แอปปิดแล้วเปิดใหม่เอง มุมซ้ายล่างเปลี่ยนเป็น `v1.0.1`

ถ้าเห็นครบตามนี้ = ระบบสมบูรณ์ จบงาน

**ถ้าไม่มีอะไรขึ้น** → เมนู `ช่วยเหลือ → ตรวจหาอัปเดต` จะเด้ง dialog บอกสาเหตุ และดู [8.4](#84-แอปไม่เจออัปเดต)

---

## ส่วนที่ 6 — งานประจำวัน: แก้โค้ด → ปล่อยอัปเดต

หลังตั้งค่าเสร็จแล้ว รอบต่อ ๆ ไปเหลือแค่ 4 คำสั่ง

```powershell
# 1. แก้ src/renderer/index.html หรือไฟล์อื่นตามต้องการ

# 2. ขึ้นเลขเวอร์ชันใน package.json  (1.0.1 -> 1.0.2)

# 3. push + tag
git add -A
git commit -m "v1.0.2 แก้ฟอร์มใบวางบิล"
git tag v1.0.2
git push origin main --tags

# 4. รอ Actions เขียว — จบ
```

ผู้ใช้ทุกคนจะได้อัปเดตภายใน 4 ชั่วโมง หรือทันทีที่เปิดแอปใหม่

### กติกาเลขเวอร์ชัน

- เลขใน `package.json` กับชื่อ tag **ต้องตรงกัน** → `"1.0.2"` คู่กับ `v1.0.2`
- เลขต้อง**สูงขึ้นเสมอ** ตามหลัก [semver](https://semver.org/lang/th/) — แอปเทียบด้วยตัวเลข ไม่ใช่วันที่
  - แก้ bug เล็ก ๆ → `1.0.1` → `1.0.2`
  - เพิ่มฟีเจอร์ → `1.0.2` → `1.1.0`
  - รื้อใหญ่ → `1.1.0` → `2.0.0`
- tag ซ้ำไม่ได้ ถ้าเผลอ tag ผิดให้ลบก่อน:

```powershell
git tag -d v1.0.2
git push origin :refs/tags/v1.0.2
```

### แก้แค่หน้าเว็บ ไม่ต้องแก้อย่างอื่น

`src/renderer/index.html` คือไฟล์ `CP9X.html` เดิมทั้งดุ้น แก้ได้ตามสบาย
**ข้อห้ามเดียว:** อย่าลบ 2 บรรทัดท้ายไฟล์ก่อน `</body>`

```html
<!-- CP9X Desktop: updater UI -->
<script src="./updater-ui.js"></script>
```

ลบแล้วแถบแจ้งเตือนอัปเดตจะหายไป (ตัวอัปเดตยังทำงานเงียบ ๆ อยู่ แต่ผู้ใช้จะไม่เห็นอะไรเลย)

---

## ส่วนที่ 7 — เข้าใจว่ามันทำงานยังไง

### 7.1 ไฟล์ไหนทำอะไร

```
cp9x-desktop/
├─ package.json              เลขเวอร์ชัน + รายการ dependency + คำสั่ง npm
├─ package-lock.json         ล็อกเวอร์ชันเป๊ะ ๆ (สร้างจาก npm install)
├─ electron-builder.yml      สูตร build .exe + บอกว่าอัปเดตจากที่ไหน
├─ build-exe.bat             ดับเบิลคลิกแล้ว build ให้เลย
├─ build/icon.ico            ไอคอนแอป — เปลี่ยนได้ ต้องเป็น .ico มี 256x256
├─ src/
│  ├─ main.js                สมองของแอป: เปิดหน้าต่าง + เมนู + ตรรกะอัปเดต
│  ├─ preload.js             สะพานปลอดภัยระหว่างหน้าเว็บกับ Node
│  └─ renderer/
│     ├─ index.html          หน้า UI (= CP9X.html)
│     └─ updater-ui.js       แถบแจ้งเตือนอัปเดตมุมขวาล่าง
└─ .github/workflows/
   └─ release.yml            สั่ง GitHub ให้ build+ปล่อย เมื่อเจอ tag ขึ้นต้นด้วย v
```

### 7.2 วงจรอัปเดต

```
คุณ push tag v1.0.2
        ↓
GitHub Actions รัน windows-latest
        ↓
npm install → electron-builder --publish always
        ↓
สร้าง Release v1.0.2 แนบ .exe + .blockmap + latest.yml
        ↓
        ⋮ (แอปของผู้ใช้)
        ↓
เปิดแอปครบ 5 วิ / หรือครบรอบ 4 ชม.
        ↓
ยิง GET ไปอ่าน latest.yml บน Release ล่าสุด
        ↓
เทียบเลขเวอร์ชันกับตัวเอง — สูงกว่า?
        ↓ ใช่
ดาวน์โหลด .exe เบื้องหลัง (ใช้ .blockmap โหลดเฉพาะส่วนที่ต่าง)
        ↓
ตรวจ SHA512 ที่ระบุใน latest.yml ว่าไฟล์ไม่ถูกแก้ระหว่างทาง
        ↓
ขึ้นแถบ "พร้อมติดตั้ง"
        ↓
กดรีสตาร์ต → ติดตั้งทับ → เปิดแอปใหม่
   หรือ ไม่กด → ติดตั้งให้เองตอนปิดแอปครั้งถัดไป
```

### 7.3 ตารางจังหวะการตรวจอัปเดต

| เงื่อนไข | พฤติกรรม | แก้ได้ที่ |
|---|---|---|
| เปิดแอปครบ 5 วินาที | ตรวจครั้งแรก | `main.js` บรรทัด `setTimeout(... 5000)` |
| ทุก 4 ชั่วโมง | ตรวจซ้ำ | `main.js` บรรทัด `setInterval(... 4 * 60 * 60 * 1000)` |
| เจอของใหม่ | ดาวน์โหลดทันทีอัตโนมัติ | `autoUpdater.autoDownload = true` |
| ผู้ใช้กด "ไว้ทีหลัง" | ติดตั้งตอนปิดแอป | `autoUpdater.autoInstallOnAppQuit = true` |
| เมนู ช่วยเหลือ → ตรวจหาอัปเดต | ตรวจทันที + เด้ง dialog บอกผล | `main.js` ฟังก์ชัน `checkForUpdates(true)` |
| รันด้วย `npm run dev` | ข้ามการตรวจทั้งหมด | ตัวแปร `isDev` |

### 7.4 ความปลอดภัยที่ตั้งไว้แล้ว

- `contextIsolation: true` + `nodeIntegration: false` — หน้าเว็บแตะ Node.js/ระบบไฟล์ตรง ๆ ไม่ได้ ต้องผ่าน `preload.js` ที่เปิดเฉพาะ 5 ฟังก์ชันเท่านั้น
- ลิงก์ภายนอกเปิดด้วยเบราว์เซอร์ของเครื่อง ไม่เปิดในหน้าต่างแอป (กันหน้าแปลกปลอมสวมรอย UI)
- single instance lock — เปิดซ้ำจะเด้งหน้าต่างเดิมขึ้นมาแทนที่จะเปิดใหม่
- ตัวอัปเดตตรวจ SHA512 ทุกครั้งก่อนติดตั้ง

---

## ส่วนที่ 8 — แก้ปัญหา

### 8.1 `npm : ไม่รู้จักคำสั่ง`

ยังไม่ได้ติดตั้ง Node หรือยังไม่ได้ปิด-เปิด PowerShell ใหม่ → กลับไป [0.1](#01-ติดตั้ง-nodejs)

### 8.2 PowerShell บล็อกสคริปต์ (`running scripts is disabled`)

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

พิมพ์ `Y` ยืนยัน แล้วลองใหม่

### 8.3 Windows SmartScreen เตือน "Unknown publisher"

ปกติของแอปที่ไม่ได้เซ็นโค้ด กด **More info → Run anyway**

จะให้หายถาวรต้องซื้อ Code Signing Certificate:

| แบบ | ราคาโดยประมาณ | ผล |
|---|---|---|
| OV (Organization Validation) | 5,000–12,000 บาท/ปี | ชื่อบริษัทขึ้น แต่ SmartScreen ยังเตือนช่วงแรกจนกว่าจะสะสมชื่อเสียง |
| EV (Extended Validation) | 12,000–25,000 บาท/ปี | ไม่เตือนตั้งแต่วันแรก ต้องใช้ USB token |

ได้ไฟล์ `.pfx` มาแล้วเพิ่มใน `electron-builder.yml`:

```yaml
win:
  certificateFile: cert.pfx
  certificatePassword: ${env.CSC_KEY_PASSWORD}
```

แล้วตั้ง `$env:CSC_KEY_PASSWORD` ก่อน build (อย่า commit ไฟล์ `.pfx` ขึ้น git เด็ดขาด)

### 8.4 แอปไม่เจออัปเดต

ไล่เช็คตามลำดับ:

1. **รันจากตัวติดตั้งหรือเปล่า** — `npm run dev` จะไม่ตรวจอัปเดตโดยตั้งใจ
2. **`owner` ใน `electron-builder.yml` ถูกไหม** — ยังเป็น `CHANGE_ME_GITHUB_USER` อยู่หรือเปล่า และ**ได้ build ใหม่หลังแก้แล้วหรือยัง**
3. **repo เป็น Public ไหม** — Private ต้องมี token
4. **Release เป็น draft หรือ pre-release หรือเปล่า** — ต้องเป็น published release ปกติ ถ้าเป็น draft ให้เข้าไปกด *Publish release*
5. **มี `latest.yml` แนบใน Release ไหม** — ขาดไฟล์นี้แอปหาไม่เจอ
6. **เลขเวอร์ชันสูงกว่าตัวที่ติดตั้งจริงไหม**
7. **ดู log** — เมนู `ช่วยเหลือ → เปิดโฟลเดอร์ log` หรือเปิด `%APPDATA%\CP9X\logs\main.log` มองหาบรรทัด `Found version` / `Checking for update` / `Error:`

### 8.5 GitHub Actions ล้มเหลว

เข้าแท็บ Actions → คลิกงานที่แดง → กางขั้นที่แดงอ่าน error

| ข้อความ | สาเหตุ | แก้ |
|---|---|---|
| `Dependencies lock file is not found` | ไม่ได้ commit `package-lock.json` | รัน `npm install` แล้ว `git add package-lock.json` แล้ว commit push |
| `Resource not accessible by integration` | Actions ไม่มีสิทธิ์เขียน Release | Settings → Actions → General → Workflow permissions → เลือก **Read and write permissions** |
| `Release already exists` / `tag already exists` | tag ซ้ำ | ลบ tag เดิม (ดู [ส่วนที่ 6](#กติกาเลขเวอร์ชัน)) แล้ว tag ใหม่ |
| `cannot find icon.ico` | ลืม commit `build/icon.ico` | `git add -f build/icon.ico` |

### 8.6 แอปเปิดมาจอขาวเปล่า

เปิด DevTools (`Ctrl+Shift+I`) → tab Console ดู error สีแดง
สาเหตุที่พบบ่อยคือไฟล์ `index.html` เสียหายจากการแก้ หรือ path ไม่ตรง

### 8.7 ฟอนต์/ไอคอนไม่ขึ้น หน้าตาเพี้ยน

หน้าเว็บโหลด Tailwind, Kanit, Font Awesome จาก CDN → **ต้องต่อเน็ต**
ถ้าต้องใช้ออฟไลน์ ต้องโหลดไฟล์เหล่านั้นมาเก็บใน `src/renderer/` แล้วแก้ path ให้ชี้ไฟล์ในเครื่อง

### 8.8 ติด CORS เรียก Supabase ไม่ได้

Edge Function `app-api` ตั้ง `Access-Control-Allow-Origin: '*'` อยู่แล้ว จึงเรียกจาก `file://` ได้ปกติ
ถ้าวันหนึ่งไปแก้ให้เจาะจง origin จะพังทันที — ต้องอนุญาต `null` เพิ่ม เพราะ Electron ที่โหลดจาก `file://` ส่ง `Origin: null`

### 8.9 อยากถอนการติดตั้ง

Settings → Apps → Installed apps → หา CP9X → Uninstall
ข้อมูลใน `%APPDATA%\CP9X` จะไม่ถูกลบ (ตั้งไว้ที่ `deleteAppDataOnUninstall: false`) — ลบเองได้ถ้าต้องการล้างจริง

---

## ส่วนที่ 9 — ปรับแต่งเพิ่มเติม

### 9.1 เปลี่ยนไอคอน

เตรียมรูปสี่เหลี่ยมจัตุรัส แปลงเป็น `.ico` ที่มีขนาด 256×256 อยู่ข้างใน (เว็บ https://icoconverter.com ใช้ได้)
แทนที่ `build/icon.ico` แล้ว build ใหม่

### 9.2 เปลี่ยนความถี่การตรวจอัปเดต

เปิด `src/main.js` ท้ายไฟล์:

```js
setTimeout(() => checkForUpdates(false), 5000);                    // ครั้งแรก 5 วินาที
setInterval(() => checkForUpdates(false), 4 * 60 * 60 * 1000);     // ทุก 4 ชั่วโมง
```

อยากให้ตรวจทุก 30 นาที เปลี่ยนบรรทัดล่างเป็น `30 * 60 * 1000`

### 9.3 ไม่อยากให้ดาวน์โหลดเอง อยากถามก่อน

ใน `src/main.js` เปลี่ยน:

```js
autoUpdater.autoDownload = false;
```

แล้วในแถบแจ้งเตือนจะต้องเพิ่มปุ่มเรียก `window.cp9x.updater.download()` เอง (ฟังก์ชันเตรียมไว้ให้แล้วใน `preload.js`)

### 9.4 ความปลอดภัย Supabase

คีย์ที่ฝังในหน้าเว็บคือ **publishable key** (`sb_publishable_...`) ซึ่งออกแบบมาให้เปิดเผยได้
แต่แอปเดสก์ท็อปถูกแกะดูโค้ดได้ (แตกไฟล์ `app.asar` ก็เห็นหมด) ดังนั้น**ต้อง**:

- เปิด RLS (Row Level Security) ทุกตารางที่สำคัญ
- ตรวจสิทธิ์ผู้ใช้ในฝั่ง Edge Function เสมอ ไม่เชื่อค่าที่ส่งมาจาก client
- อย่าฝัง `service_role key` ในโค้ดฝั่งแอปเด็ดขาด

### 9.5 อยากได้แบบ portable ไม่ต้องติดตั้ง

เพิ่มใน `electron-builder.yml`:

```yaml
win:
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
```

> ไฟล์ portable **อัปเดตตัวเองไม่ได้** — ต้องโหลดใหม่เองทุกครั้ง

### 9.6 คำสั่ง npm ทั้งหมด

| คำสั่ง | ทำอะไร |
|---|---|
| `npm install` | ติดตั้ง dependencies |
| `npm run dev` | เปิดแอปโหมดพัฒนา + DevTools |
| `npm start` | เปิดแอปแบบไม่มี DevTools |
| `npm run pack` | build แบบไม่บีบเป็น installer (เร็ว ไว้ทดสอบ) |
| `npm run build` | build `.exe` ลง `dist\` |
| `npm run release` | build + upload ขึ้น GitHub Release (ต้องมี `GH_TOKEN`) |
