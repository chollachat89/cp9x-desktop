# วิธีปล่อยอัพเดท CP9X แบบกันพลาด

## สิ่งที่เกิดขึ้นกับ v1.0.58 (ตรวจจาก GitHub จริงแล้ว)

**v1.0.58 ไม่เคยถูกปล่อยจริง** — ตอนนี้ทุกเครื่องยังอยู่ที่ **v1.0.57**

| หลักฐาน | สิ่งที่พบ |
|---|---|
| หน้า Releases | **v1.0.57 ขึ้นเป็น Latest** · v1.0.58 ไม่อยู่ในรายการ |
| หน้า v1.0.58 | มี **Assets แค่ 2** (ไฟล์ source zip/tar ที่ GitHub แถมให้เอง) · **ไม่มี .exe ไม่มี latest.yml** |
| Release อื่นที่ปกติ | มี **Assets 5** ทุกอัน (.exe + .blockmap + latest.yml + source 2 ไฟล์) |
| `package.json` ที่ tag v1.0.58 | เขียนว่า **`"version": "1.0.59"`** |

**สาเหตุ:** tag ชื่อ `v1.0.58` แต่ `package.json` ใน commit นั้นเป็น `1.0.59`

ใน workflow มีด่านตรวจว่า "tag ต้องตรงกับ package.json" พอไม่ตรงมันก็**หยุดทันที ไม่ build อะไรเลย** เหลือแค่ tag เปล่า ๆ ค้างไว้บน GitHub

ด่านนี้**ทำงานถูกต้องแล้ว** — มันกันไม่ให้ปล่อยของที่เลขเวอร์ชันเพี้ยนออกไป ถ้าไม่มีด่านนี้ ผู้ใช้จะได้ไฟล์ที่ข้างในเป็น 1.0.59 แต่ป้ายบอกว่า 1.0.58 แล้วระบบอัปเดตจะรวนไปตลอด

---

## แก้ตอนนี้ — ปล่อย v1.0.59

โค้ดทั้งหมดพร้อมแล้ว (`package.json` เป็น 1.0.59 อยู่แล้ว) แค่สร้าง tag ให้ตรงแล้ว push

```powershell
cd "C:\Users\Haneef\Downloads\ใบวางบิลอัพเกรด\cp9x-desktop"

git add -A
git commit -m "v1.0.59"
git tag v1.0.59
git push origin main --follow-tags
```

> ถ้าขึ้น `nothing to commit` แปลว่า commit ไปแล้ว ข้ามไปบรรทัด `git tag` ได้เลย

**เช็คว่าสำเร็จจริง** — เข้า https://github.com/chollachat89/cp9x-desktop/releases
แล้วดูว่า **v1.0.59 ขึ้นเป็น Latest** และมี **Assets 5** (ต้องเห็น `latest.yml` ด้วย ไม่งั้นอัปเดตไม่ทำงาน)

---

## ลบ tag เปล่า v1.0.58 ทิ้ง (ไม่บังคับ)

ปล่อยไว้ก็ไม่มีผลเสีย ระบบอัปเดตมองไม่เห็นอยู่แล้วเพราะไม่มี `latest.yml` แต่ถ้าอยากให้หน้า Releases สะอาด:

```powershell
git push --delete origin v1.0.58
git tag -d v1.0.58
```

---

## รอบต่อไปใช้คำสั่งเดียวจบ

เพิ่มคำสั่งใหม่ไว้ให้แล้ว ใช้แทนการแก้เลขเองแล้ว tag มือ:

```powershell
npm run ship
```

คำสั่งนี้ทำให้ 4 อย่างพร้อมกัน จึง**ไม่มีทางที่ tag กับ package.json จะไม่ตรงกัน**:

1. ขึ้นเลขเวอร์ชันใน `package.json` ให้ (1.0.59 → 1.0.60)
2. ขึ้นเลขใน `package-lock.json` ให้ด้วย
3. commit ให้
4. สร้าง tag ชื่อ `v<เลขใหม่>` แล้ว push ทั้ง commit และ tag

ถ้าอยากขึ้นเลขกลาง (1.0.x → 1.1.0) ใช้ `npm run ship:minor`

> **ก่อนรัน `npm run ship` ต้อง commit งานที่แก้ไว้ให้หมดก่อน** ไม่งั้น npm จะไม่ยอมทำงาน (เป็นการกันพลาดของ npm เอง)

---

## เช็คลิสต์ก่อนปล่อยทุกครั้ง

1. งานที่แก้ commit ครบแล้ว (`git status` ต้องว่าง)
2. ถ้ารอบนั้นมี SQL ใหม่ → **รัน SQL ก่อน** แล้วค่อย deploy Edge Function
3. ถ้ารอบนั้นแก้ `index.ts` → **deploy Edge Function ก่อน** แล้วค่อยปล่อยแอป
4. `npm run ship`
5. ดูหน้า Actions ต้องขึ้นเขียว (ราว 2 นาที)
6. ดูหน้า Releases ต้องมี **Assets 5** และเวอร์ชันใหม่ขึ้นเป็น **Latest**

---

## อาการเวลาพลาด กับวิธีดูว่าพลาดแบบไหน

| อาการที่เห็น | แปลว่า | แก้ยังไง |
|---|---|---|
| Release มี Assets แค่ 2 | workflow ไม่ได้ build (มักเพราะ tag ไม่ตรง package.json) | สร้าง tag ใหม่ให้ตรง แล้ว push |
| ไม่มี Release ขึ้นเลย | tag ยังไม่ได้ push ขึ้น GitHub | `git push origin --tags` |
| มี Assets 5 แต่เครื่องไม่อัปเดต | ไฟล์ `latest.yml` หายไปจาก Release | รัน workflow ใหม่ อย่าลบ latest.yml ออกจาก Release เด็ดขาด |
| เครื่องขึ้นเวอร์ชันไม่ตรงกับที่ปล่อย | tag กับ package.json ไม่ตรงกันตั้งแต่แรก | ปล่อยเลขใหม่ทับลงไป **ห้ามปล่อยทับเลขเดิม** |

⚠ **ห้ามลบ Release เก่าแล้วปล่อยเลขเดิมซ้ำ** — auto-update เทียบกันด้วยเลขเวอร์ชันอย่างเดียว เครื่องที่ติดตั้งเลขนั้นไปแล้วจะมองว่าตัวเองใหม่สุด แล้วจะไม่อัปเดตอีกเลย · ต้องปล่อยเลขใหม่ทับลงไปเสมอ
