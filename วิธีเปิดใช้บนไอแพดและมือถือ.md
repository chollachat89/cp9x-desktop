# เปิด CP9X บน iPad และมือถือ — ทำตามทีละขั้น

ทำครั้งเดียวจบ หลังจากนั้นเว็บจะอัปเดตตามอัตโนมัติทุกครั้งที่ push โค้ดขึ้น GitHub

---

## หลักการ: ใช้ไฟล์ชุดเดียวกับแอปเดสก์ท็อป

แอป CP9X เป็นหน้าเว็บอยู่แล้วโดยเนื้อแท้ — `src/renderer/index.html` คุยกับเซิร์ฟเวอร์ผ่าน `fetch()` ตรง ๆ ไม่มีโค้ดที่ผูกกับ Electron เลย

ดังนั้น **ไม่ต้องแยกโค้ดเป็น 2 ชุด** เอาโฟลเดอร์ `src/renderer` ไปวางบนเว็บตรง ๆ ได้เลย

| | แอปเดสก์ท็อป | เว็บบน iPad/มือถือ |
|---|---|---|
| ไฟล์ที่ใช้ | `src/renderer/index.html` | ไฟล์เดียวกัน |
| อัปเดตยังไง | ติดตั้งตัวใหม่ทับ (electron-updater) | รีเฟรชหน้าเว็บ |
| เซิร์ฟเวอร์ | Supabase Edge Function ตัวเดียวกัน | ตัวเดียวกัน |

แก้โค้ดครั้งเดียว ได้ทั้ง 2 ทาง

---

## ขั้นที่ 1 — สร้างโปรเจกต์บน Cloudflare Pages

1. สมัคร/เข้า https://dash.cloudflare.com (ฟรี)
2. เมนูซ้าย เลือก **Workers & Pages** แล้วกด **Create** แท็บ **Pages**
3. กด **Connect to Git** แล้วเลือก repo `chollachat89/cp9x-desktop`
4. ตั้งค่าตามนี้ **เป๊ะ ๆ**

   | ช่อง | ใส่ว่า |
   |---|---|
   | Project name | `cp9x` |
   | Production branch | `main` |
   | Framework preset | `None` |
   | Build command | **เว้นว่างไว้** |
   | Build output directory | `src/renderer` |

   > **Build command ต้องว่าง** และ **Build output directory ต้องเป็น `src/renderer`**
   > เพราะโปรเจกต์นี้ไม่ต้อง build อะไรเลย Cloudflare แค่เอาไฟล์ในโฟลเดอร์นั้นไปวางตรง ๆ
   > ถ้าใส่ build command มั่ว ๆ ไว้ จะ deploy ไม่ผ่านและขึ้น error ที่อ่านไม่รู้เรื่อง

5. กด **Save and Deploy** รอสักครู่จะได้ URL หน้าตาแบบ `https://cp9x.pages.dev`

เสร็จแล้ว เปิด URL นั้นบนคอมพิวเตอร์ดูก่อนว่าขึ้นหน้า login ปกติ

---

## ขั้นที่ 2 — ติดตั้งลงหน้าจอโฮมของ iPad / iPhone

1. เปิด URL ด้วย **Safari** (ต้อง Safari เท่านั้น Chrome บน iOS ทำขั้นนี้ไม่ได้)
2. กดปุ่ม **แชร์** (สี่เหลี่ยมมีลูกศรขึ้น)
3. เลื่อนลงเลือก **เพิ่มไปยังหน้าจอโฮม**
4. กด **เพิ่ม**

จะได้ไอคอน CP9X บนหน้าจอเหมือนแอปจริง กดเปิดแล้วเต็มจอ ไม่มีแถบ URL

### บน Android

เปิด URL ด้วย **Chrome** แล้วกดปุ่ม **"ติดตั้งลงหน้าจอโฮม"** ที่ท้ายเมนูซ้าย (ปุ่มจะโผล่เองเมื่อติดตั้งได้)

หรือรอ Chrome เด้งแถบ "ติดตั้งแอป" ขึ้นมาเองก็ได้ ผลเหมือนกัน

> **ถ้าเคยเพิ่มไอคอนไว้ก่อน v1.0.66** ให้ลบไอคอนเดิมทิ้งแล้วเพิ่มใหม่
> เพราะไอคอนเดิมจำค่าการแสดงผลแบบเก่าไว้ (แถบสถานะทับหัวแอป) รีเฟรชหน้าเว็บเฉย ๆ ไม่พอ

---

## ขั้นที่ 3 — อัปเดตเว็บในครั้งต่อ ๆ ไป

ไม่ต้องทำอะไรเพิ่ม **push ขึ้น `main` แล้ว Cloudflare build ให้เองภายในไม่กี่สิบวินาที**

ผู้ใช้แค่ปิดแล้วเปิดแอปใหม่ก็ได้ของใหม่ทันที (ตั้ง `Cache-Control: no-cache` ไว้ในไฟล์ `_headers` แล้ว จึงไม่ค้างเวอร์ชันเก่า)

> แอปเดสก์ท็อปยังอัปเดตด้วยการ tag `v1.0.xx` เหมือนเดิมทุกอย่าง ไม่เกี่ยวข้องกัน

---

## ใช้งานได้แค่ไหนบนแต่ละเครื่อง

| หน้าจอ | สถานะ |
|---|---|
| iPad แนวนอน (1024px+) | ใช้ได้เต็มรูปแบบเหมือนคอมพิวเตอร์ |
| iPad แนวตั้ง | ใช้ได้ เมนูซ้ายยุบเป็นปุ่มขีดสามขีด |
| มือถือ — เมนูอนุมัติไฟล์ผู้รับเหมา | **ปรับให้เป็นการ์ดแล้ว** กดอนุมัติ/ตีกลับได้สบาย |
| มือถือ — ตารางวางบิล / รายงานสถานะ | ดูข้อมูลได้ เลื่อนซ้ายขวา คอลัมน์แรกตรึงไว้ให้ · แก้ไขข้อมูลแนะนำให้ใช้คอมพิวเตอร์ |
| มือถือ — ฟอร์มเปิดงาน/ปิดงาน | ยังไม่ได้ปรับ ใช้ได้แต่กรอกลำบาก |

### ข้อจำกัดที่ยังแก้ไม่ได้ในรอบนี้

**ปุ่มดาวน์โหลดไฟล์ (Excel / PDF / CSV) บน iPhone และ iPad ยังใช้ไม่ได้**

สาเหตุ: โค้ดสร้างไฟล์ในเครื่องแล้วสั่งดาวน์โหลดด้วย `a.download` ซึ่ง Safari บน iOS ไม่รองรับ เป็นข้อจำกัดของ iOS เอง ไม่ใช่บั๊กของแอป

แก้ได้ แต่ต้องเปลี่ยนวิธีส่งไฟล์ให้อัปโหลดขึ้น Supabase Storage ก่อนแล้วเปิดเป็นลิงก์แทน — เป็นงานอีกรอบหนึ่ง

**ระหว่างนี้:** ให้โหลดไฟล์จากคอมพิวเตอร์ · ส่วนบนมือถือใช้ดูข้อมูลและกดอนุมัติได้ตามปกติ

---

## เรื่องความปลอดภัย

เว็บนี้**เปิดสาธารณะ** ใครรู้ URL ก็เข้าถึงหน้า login ได้ แต่เข้าระบบไม่ได้ถ้าไม่มีรหัส — เหมือนแอปเดสก์ท็อปตอนนี้ทุกอย่าง เพราะใช้ระบบ login และเซิร์ฟเวอร์ตัวเดียวกัน

> ถ้าภายหลังอยากกันเพิ่มอีกชั้น Cloudflare Pages มี **Cloudflare Access** ให้เปิดได้ฟรี
> จะบังคับให้ยืนยันตัวตนด้วยอีเมลขององค์กรก่อนถึงจะเห็นหน้า login ของ CP9X
> ตั้งที่ Workers & Pages > โปรเจกต์ > Settings > Access policy
